<article  class="card">
    <a href="/blogs/{{id}}" >
        <header>
            <h2>{{title}}</h2>
            <p>Posted by {{user.username}} {{createdAt}}</p>
        </header>

        <div>
            <p>{{content}}</p> 
        </div>
    </a>
</article>


